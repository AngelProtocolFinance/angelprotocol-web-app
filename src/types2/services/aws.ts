export enum awsTags {
  airdrop = "airdrop",
  admin = "admin",
  cha = "charity",
}

export enum chaTags {
  profile = "profile",
  profiles = "profiles",
}

export enum adminTags {
  applications = "applications",
}
