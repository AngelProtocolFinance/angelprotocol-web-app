export enum tags {
  gov = "gov",
  user = "user",
  halo = "halo",
}

export enum gov {
  polls = "polls",
  state = "state",
  staker = "staker",
  config = "config",
  halo_balance = "halo_balance",
}

export enum user {
  terra_balance = "terra_balance",
  halo_balance = "halo_balance",
}

export enum halo {
  info = "info",
}
