export enum tags {
  airdrop = "airdrop",
  cha = "charity",
  alliance = "alliance",
  admin = "admin",
}

export enum cha {
  profile = "profile",
  profiles = "profiles",
}

export enum alliance {
  members = "members",
}

export enum admin {
  applications = "applications",
}
