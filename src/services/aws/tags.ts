export enum awsTags {
  airdrop = "airdrop",
  admin = "admin",
  profile = "profile",
  endowments = "endowments",
}

export enum chaTags {
  profile = "profile",
  profiles = "profiles",
}

export enum adminTags {
  applications = "applications",
  registration = "registration",
}
