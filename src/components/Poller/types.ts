export interface Values {
  amount: string;
  title: string;
  description: string;
  link: string;
  //execute_msgs: predefined fields

  //metadata;
  fee: number;
  loading: boolean;
  form_error: string;
}
