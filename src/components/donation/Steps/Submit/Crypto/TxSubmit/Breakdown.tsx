import { humanize } from "helpers";
import { CryptoSubmitStep } from "slices/donation";
import { Row } from "../Row";

export default function Breakdown({ details }: CryptoSubmitStep) {
  const token = details.token;

  return <></>;
}
