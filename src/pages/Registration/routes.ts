enum routes {
  index = "/",
  contactDetails = "contact-details",
  status = "status",
  confirm = "confirm",
  verify = "verify/:token",
  wallet = "wallet",
  charityProfile = "charity-profile",
}

export default routes;
