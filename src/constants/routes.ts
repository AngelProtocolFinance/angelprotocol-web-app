export enum web {
  index = "",
  charities = "for-charities",
  donors = "for-donors",
  about = "about",
  contact = "contact",
}

export enum site {
  home = "/",
  app = "/app",
}

export enum app {
  index = "/",
  marketplace = "marketplace",
  leaderboard = "leaderboard",
  register = "register",
  login = "login",
  charity = "charity",
  tca = "tca",
  donations = "donations",
}

export enum charity {
  index = "",
  endowment = "endowment",
  programs = "programs",
  media = "media",
  governance = "governance",
}
